extends ../layout 

block content 
  div(style={'padding': '40px', 'color': '#343A40', 'line-height': '160%', 'background': '#F1F3F5'})
    h3(style={'margin': '0 0 10px 0', 'font-size': '1.2rem'}) #{name}님, 안녕하세요!
    p(style={'margin': '0', 'font-size': '1rem'}) 축하드립니다!
    p(style={'margin': '0', 'font-size': '1rem'}) 제출하신 '#{project.title}' 프로젝트가
    p(style={'margin': '0', 'font-size': '1rem'}) 성공적으로 승인되었습니다.
    p(style={'margin': '0', 'font-size': '1rem'}) 프로젝트는 모금 진행중인 상태로 웹 사이트에 노출됩니다.
    p(style={'margin': '0', 'font-size': '1rem'}) 프로젝트 모금은 승인 후 #{project.term}일 동안 진행됩니다.
    p(style={'margin': '0 0 30px 0', 'font-size': '1rem'})  감사합니다!
    a(href=siteURL + '/detail-project/' + project.path style={'display': 'inline-block', 'padding': '15px 25px', 'font-size': '1.2rem', 'font-weight': '500', 'border-radius': '5px', 'color': '#ffffff', 'background': '#7950F2', 'text-decoration': 'none'}) 진행중인 프로젝트 확인하기
